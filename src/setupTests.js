// setupTests.js

// --------------------------------------------
// File ini otomatis dijalankan oleh Create React App (CRA)
// sebelum semua file test dijalankan oleh Jest.
// Digunakan untuk konfigurasi global testing.
// --------------------------------------------

// Import pustaka `jest-dom` dari Testing Library
// Ini menambahkan matcher tambahan seperti:
// - expect(element).toHaveTextContent(...)
// - expect(element).toBeInTheDocument()
// - expect(element).toHaveAttribute(...)
// Sehingga assertions lebih natural dan cocok untuk DOM testing

import '@testing-library/jest-dom';

// Dokumentasi lengkap:
// https://github.com/testing-library/jest-dom
